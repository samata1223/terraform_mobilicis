1.Make sure install Boto3 library using "pip install boto3"
2.Configure your AWS credentials before running this script. 
3.Replace 'instance_id' with the actual instance ID for which you want to create the alarm. Additionally, you can customize the alarm parameters as per your requirements.
